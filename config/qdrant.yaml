version: 1.0

storage:
  path: /qdrant/storage
  enable_mmap: true

# Default configuration for the service
service:
  host: 0.0.0.0
  port: 6333

# Logging configuration
log:
  level: INFO
  path: /qdrant/logs